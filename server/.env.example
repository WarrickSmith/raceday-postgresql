# Environment
NODE_ENV=development

# Database (PostgreSQL 18 - existing instance)
# Format: postgresql://[user]:[password]@[host]:[port]/[database]
# The 'raceday' database will be created in Story 1.2 via migration script
# For initial testing, use the default 'postgres' database with default credentials
# Default credentials: postgres/postgres (standard PostgreSQL defaults)
# pgAdmin access: admin@admin.com/admin (web interface for administration)
# pgAgent installed: pgagent/pgagent (job scheduling agent)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/postgres

# NZ TAB API Configuration
# Base URL for NZ TAB racing data API (public API, no key required)
NZTAB_API_URL=https://api.tab.co.nz

# Server Configuration
PORT=7000                       # Server port (default: 7000)
LOG_LEVEL=info                  # Logging level: debug, info, warn, error

# Performance Tuning
UV_THREADPOOL_SIZE=8            # Node.js thread pool size for async I/O operations
MAX_WORKER_THREADS=3            # Worker threads for CPU-intensive tasks
DB_POOL_MAX=10                  # Maximum PostgreSQL connection pool size
